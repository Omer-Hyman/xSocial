import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.friedOmelette.app',
  appName: 'xSocial',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
